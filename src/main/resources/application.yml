spring:
  datasource:
    url: jdbc:postgresql://localhost:54321/mayak-test
    username: "postgres"
    password: "1"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
